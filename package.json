{
  "name": "better-qr",
  "version": "0.2.0",
  "description": "No-runtime-dependency TypeScript QR code generator with SVG rendering and optional center icons from your own.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./render-model": {
      "types": "./dist/render-model.d.ts",
      "import": "./dist/render-model.js"
    }
  },
  "files": [
    "dist/index.js",
    "dist/index.js.map",
    "dist/index.d.ts",
    "dist/render-model.js",
    "dist/render-model.js.map",
    "dist/render-model.d.ts",
    "dist/qr-code.d.ts",
    "dist/svg.d.ts",
    "dist/types.d.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "rollup -c && tsc -p tsconfig.types.json",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint --deny-warnings .",
    "lint:fix": "oxlint --fix .",
    "prepack": "npm run build",
    "coverage": "vitest run --coverage",
    "test": "vitest run",
    "test:all": "npm test && npm --prefix packages/react test && npm --prefix packages/vue test && npm --prefix packages/angular test",
    "check": "npm run format:check && npm run lint && npm run build && npm run coverage"
  },
  "keywords": [
    "qr",
    "qrcode",
    "iso-iec-18004",
    "svg",
    "typescript"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/wrknbuycnsmndie/better-qr"
  },
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.3.0",
    "@vitest/coverage-v8": "^4.1.4",
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0",
    "rollup": "^4.60.1",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.4"
  }
}
